% LTeX: enabled=false
%%-----------------------------------------
%% packages must be loaded first
%%-----------------------------------------
%%%
\usepackage{calc} % Allow arithmetic in LATEX commands
%%% Color
\usepackage[%
  table,      % Load the colortbl package
  hyperref,   % Support  the  hyperref  package
]{xcolor}
%%% Graphic
\usepackage[%
  %final,   % default
  %draft,   % not include graphics
]{graphicx}
%%% Language
% \usepackage[english]{babel}
%%% Enumitem
\usepackage[%
	shortlabels,% snippt of labels
	inline,% predefined inline lists
]{enumitem}

